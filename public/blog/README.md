Images embedded in blog post bodies.

Referenced from posts as absolute paths, e.g. `/blog/gomemlimit-before.png`.
Cover images (used for OG previews) live in `../cover/` instead, named
`cover-<value of the post's `cover:` frontmatter>`.
